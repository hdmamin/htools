from setuptools import setup


setup(name='htools',
      version='0.1',
      description='Harrison\'s custom functions.',
      packages=['htools'],
      author='Harrison Mamin',
      zip_safe=False)
